import { Layout } from "@/components/layout";
import { NextPage } from "next";

const Classes: NextPage = () => {
  //Get classes

  return (
    <Layout>
      <h1 className="text-2xl">Classes</h1>
      {/* Select with classes list */}
      {/* Show class infos */}
    </Layout>
  );
};

export default Classes;
