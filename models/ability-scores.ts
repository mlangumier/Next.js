export interface IAbilityScore {
  index: string;
  name: string;
  url: string;
}

export interface IScore {
  name: string;
  score: number;
}

export interface IAbilityScoreInfo {
  index: string;
  name: string;
  full_name: string;
  desc: string[];
}
